---
title: Authentification | Documentation de l'API Smartbills
description: Découvrez les méthodes d'authentification fournies par Smartbills, y compris les flux OAuth2 pour un accès sécurisé.
---
import { Tabs } from 'nextra/components'
import { Endpoints } from '../components/Endpoints'
import { Attributes } from '../components/Attributes'

## Authentification
L'API Smartbills utilise des jetons d'accès (bearer tokens) pour authentifier les requêtes. Vous pouvez consulter et gérer vos clés d'API dans le tableau de bord Smartbills.

Vous pouvez utiliser des clés d'API restreintes pour des permissions granulaire.

Vos clés d'API confèrent de nombreux privilèges, veillez donc à les garder en sécurité ! Ne partagez pas vos clés d'API secrètes dans des zones accessibles au public, comme GitHub, dans du code côté client, etc.

Toutes les requêtes API doivent être effectuées via HTTPS. Les appels effectués en HTTP simple échoueront. Les requêtes API sans authentification échoueront également.

## Authentification OAuth2

###  Flux server à serveur
Utilisez ce processus pour la communication serveur à serveur.

**Endpoint:** `https://api.smartbills.io/connect/token`

**Requête:**
```http
POST /connect/token HTTP/1.1
Host: api.smartbills.io
Content-Type: application/x-www-form-urlencoded

grant_type=client_credentials&client_id=VOTRE_CLIENT_ID&client_secret=VOTRE_CLIENT_SECRET
```

**Réponse:**
```json
{
  "access_token": "VOTRE_ACCESS_TOKEN",
  "token_type": "Bearer",
  "expires_in": 3600
}
```

#### Exemple de code

<Tabs items={['JS', '.NET']}>

  <Tabs.Tab>
  ```js copy
  import { SBClient } from "@smartbills/sdk"
  
  const client = new SBClient();
  const tokenResponse = await client.auth.getToken({
    grant_type: "client_credentials",
    client_id: "VOTRE_CLIENT_ID",
    client_secret: "VOTRE_CLIENT_SECRET"
  });
  ```
  </Tabs.Tab>
  <Tabs.Tab>
  ```c# copy
  using Smartbills.NET;
  
  var client = new SBClient();
  var tokenResponse = await client.Auth.GetTokenAsync(new TokenRequest
  {
      GrantType = "client_credentials",
      ClientId = "VOTRE_CLIENT_ID",
      ClientSecret = "VOTRE_CLIENT_SECRET"
  });
  ```
  </Tabs.Tab>
</Tabs>

### Flux du code d'autorisation
Utilisez ce flux pour l'authentification des utilisateurs.

**Étape 1: Rediriger vers le point de terminaison d'autorisation**
```http
GET /connect/authorize?response_type=code&client_id=VOTRE_CLIENT_ID&redirect_uri=VOTRE_REDIRECT_URI&scope=VOS_SCOPES
Host: api.smartbills.io
```

**Étape 2: Échanger le code d'autorisation contre un jeton d'accès**
```http
POST /connect/token HTTP/1.1
Host: api.smartbills.io
Content-Type: application/x-www-form-urlencoded

grant_type=authorization_code&code=CODE_D'AUTORISATION&redirect_uri=VOTRE_REDIRECT_URI&client_id=VOTRE_CLIENT_ID&client_secret=VOTRE_CLIENT_SECRET
```

**Réponse:**
```json
{
  "access_token": "VOTRE_ACCESS_TOKEN",
  "token_type": "Bearer",
  "expires_in": 3600,
  "refresh_token": "VOTRE_REFRESH_TOKEN"
}
```

#### Exemple de code

<Tabs items={['JS', '.NET']}>

  <Tabs.Tab>
  ```js copy
  import { SBClient } from "@smartbills/sdk"
  
  const client = new SBClient();
  const tokenResponse = await client.auth.getToken({
    grant_type: "authorization_code",
    code: "CODE_D'AUTORISATION",
    redirect_uri: "VOTRE_REDIRECT_URI",
    client_id: "VOTRE_CLIENT_ID",
    client_secret: "VOTRE_CLIENT_SECRET"
  });
  ```
  </Tabs.Tab>
  <Tabs.Tab>
  ```c# copy
  using Smartbills.NET;
  
  var client = new SBClient();
  var tokenResponse = await client.Auth.GetTokenAsync(new TokenRequest
  {
      GrantType = "authorization_code",
      Code = "CODE_D'AUTORISATION",
      RedirectUri = "VOTRE_REDIRECT_URI",
      ClientId = "VOTRE_CLIENT_ID",
      ClientSecret = "VOTRE_CLIENT_SECRET"
  });
  ```
  </Tabs.Tab>
</Tabs>