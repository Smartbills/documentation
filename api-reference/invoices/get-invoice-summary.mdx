---
title: "Get Invoice Summary"
api: "GET https://api.smartbills.io/v1/invoices/summary"
description: "Retrieve aggregated invoice metrics and statistics"
---

## Response

<ResponseField name="totalRevenue" type="number">
  Total revenue from all paid invoices
</ResponseField>

<ResponseField name="outstanding" type="number">
  Total amount of unpaid invoices
</ResponseField>

<ResponseField name="overdueCount" type="integer">
  Number of overdue invoices
</ResponseField>

<ResponseField name="paidThisMonth" type="number">
  Total amount paid in the current month
</ResponseField>

<ResponseField name="totalCount" type="integer">
  Total number of invoices
</ResponseField>

<ResponseField name="draftCount" type="integer">
  Number of draft invoices
</ResponseField>

<ResponseField name="sentCount" type="integer">
  Number of sent (awaiting payment) invoices
</ResponseField>

<ResponseField name="paidCount" type="integer">
  Number of paid invoices
</ResponseField>

<RequestExample>

```bash cURL
curl --request GET \
  --url https://api.smartbills.io/v1/invoices/summary \
  --header 'Authorization: Bearer YOUR_API_KEY' \
  --header 'Content-Type: application/json'
```

```javascript JavaScript
const summary = await smartbills.invoices.getSummary();
```

```csharp C#
var summary = await invoiceClient.GetSummaryAsync(
    new SBRequestOptions { BusinessId = 456 });
```

</RequestExample>

<ResponseExample>

```json Response
{
  "totalRevenue": 125000.00,
  "outstanding": 34500.00,
  "overdueCount": 3,
  "paidThisMonth": 18750.00,
  "totalCount": 142,
  "draftCount": 5,
  "sentCount": 12,
  "paidCount": 118
}
```

</ResponseExample>
